# 콘티 한 장

Covenant 청년부 찬양팀이 매주 쓰는 A4 찬양 콘티 편집기다. 서버 없이 `index.html` 한 파일로 돌아가는 정적 사이트다.

- 배포: https://conti-han-jang.vercel.app (Vercel 개인 Hobby 계정 `slzp300-sketch's projects`, **ImDealer 팀 아님**)
- 저장소: https://github.com/slzp300-sketch/conti-han-jang (`main` push → 자동 배포)
- 로컬: `C:\Users\upup0\Documents\conti-han-jang`
- 배포: `git add -A && git commit -m "..." && git push`, 또는 `배포하기.bat` 더블클릭

## 파일

- `index.html` — 앱 전부(HTML·CSS·JS 한 파일). 사이트용 빌드라서 Pretendard를 CDN에서 불러온다.
- `배포하기.bat` — add/commit/push를 한 번에 실행한다.
- `vercel.json`, `README.md`, `.gitignore`

## 기능

- 곡마다 **Song Form** 칸과 악보 이미지를 넣으면 A4 미리보기가 나온다. 인쇄하거나 PDF로 저장할 수 있다.
- 송폼 표기: `{C(목소리)}`처럼 중괄호로 감싸면 빨간색이 되고, `(괄호)` 안의 글자는 작게 나온다. `*`는 `B*6` 같은 반복 표기라서 **그대로 둔다**(예전에는 빨간색 표기였는데 사용자 요청으로 바꿨다).
- 송폼 칸(`.form-bar .txt`)은 **`display:block`이어야 한다.** flex로 두면 텍스트 조각과 `<span>` 하나하나가 별개의 flex 항목이 되어 각자 줄바꿈하고, `V-C-V-C-B-{C(목소리)}`가 뒤엉켜 나온다. 세로 가운데 정렬은 `align-self:center`로 한다. `min-width:0`도 필요하다 — 그리드 트랙이 자동 최소 너비라서 긴 송폼이 칸을 페이지 밖으로 밀어낸다.
- `renderForm`은 송폼을 `-` 기준으로 잘라 구획마다 `white-space:nowrap` span으로 감싼다. 줄은 **구획 사이에서만** 바뀌고 `C(목소리)`가 쪼개지지 않는다. 중괄호·괄호 안의 `-`에서는 자르지 않는다.
- `fitFormBars`는 글자 수가 아니라 **실제 렌더된 줄 수**를 보고 21→11pt로 줄인다. 한글은 두 배 폭이고 괄호 안은 작아서 글자 수로는 폭을 못 맞춘다. 폭을 예측하는 방식(`scrollWidth`, Range 측정)은 경계값에서 틀렸다 — `overflow:visible`이면 `scrollWidth`가 넘친 폭을 안 돌려주고, 미리보기가 축소돼 있어 rect 값은 축척이 섞인다.
- 악보 넣기는 이미지, **PDF**(pdf.js로 쪽마다 가로 2000px 렌더링), 드래그, Ctrl+V를 모두 지원한다.
- 자르기·크기(✂): 악보 위·아래를 %로 잘라내고 `scale`(40~100%)로 크기도 줄인다. 원본은 두고 값만 저장한다.
- 같은 페이지에 넣기: **악보마다 정한다.** 곡의 첫 악보는 `song.joinPrev`, 둘째 장부터는 `image.joinPrev`를 쓴다. 예전에는 곡의 *첫* 악보만 합칠 수 있어서, **악보가 두 장인 곡은 아무리 줄여도 두 페이지로 갈라졌다**(1쪽이 276mm 중 66mm만 쓰고 210mm를 비워 둔 채로). 페이지는 크기로 자동 배치되지 않으니, 자리가 남아도 이 스위치를 켜야 합쳐진다.
- 송폼 칸은 **한 페이지에서 곡마다 한 번만** 나온다. 같은 곡의 악보가 나란히 오면 뒤 악보는 칸을 안 그린다(`sameSongAbove`). 장이 나뉘면 각 장에 다시 붙는다.
- 두 곡 한 장: **한 페이지의 악보는 `fitSheets`가 모두 같은 배율로 그린다.** 각 악보를 페이지 폭에 맞춘 뒤, 세로가 넘치면 다 같이 같은 비율로 줄인다.
  - 예전에는 `flexGrow`로 높이를 비율 배분했는데, 그러면 **악보마다 배율이 달라져** 한쪽 곡만 크게 인쇄됐다(한쪽만 자른 경우 0.56배 대 0.64배).
  - A4 한 장짜리 악보 둘을 한 페이지에 넣으려면 각각 **높이의 절반쯤만 남겨야** 폭을 꽉 채운다. 계산은 `폭 × (r1+r2) ≤ 세로 여유`이고, 세로 여유는 276mm에서 송폼 칸과 간격을 뺀 값(둘일 때 약 236mm), `r`은 자른 뒤의 세로/가로 비다. 그래서 `r1+r2 ≤ 1.25`여야 한다.
  - 자르기 패널이 **인쇄 폭 몇 %로 들어가는지** 보여 준다(`refreshCropNote`). 100%면 꽉 찬 것이다. 이 표시가 없을 때는 얼마나 잘라야 하는지 알 길이 없어서 계속 작게 인쇄됐다.
- 편의 기능: 되돌리기·다시 실행(`Ctrl+Z`/`Ctrl+Shift+Z`), 곡 카드 접기와 모두 접기, 곡 복제, 미리보기 쪽 넘기기.
  - 되돌리기 스냅샷(`cloneState`)은 구조만 복사하고 **이미지 `src` 문자열은 참조로 공유한다.** `structuredClone`이나 JSON은 그 문자열까지 복사해서 콘티 하나에 수십 MB가 든다.
  - 스냅샷은 변경 후 600ms 쉬면 쌓인다(`markHistory`). 그러지 않으면 타자 한 글자가 한 단계가 된다. 글자 입력칸 안에서는 `Ctrl+Z`를 가로채지 않는다 — 거기선 브라우저 기본 실행취소가 맞다.
  - 곡 단위 "앞 곡과 같은 페이지" 체크박스는 `:scope > .join input`으로 고른다. 자르기 패널 안에도 같은 클래스의 악보 단위 체크박스가 있어서, 그냥 `.join input`으로 고르면 그걸 잡는다.
- 보관함: IndexedDB `conti-maker`에 저장한다. store는 `state`(key `current`)와 `contis`(keyPath `id`) 두 개다. 새 콘티·열기·복제·삭제를 지원하고, 데이터는 브라우저마다 따로 저장돼 공유되지 않는다.
- **키 바꾸기(♯)**: tesseract.js 5.1.1(CDN)로 코드를 인식하고, 캔버스에 루트와 베이스 글자만 다시 그린다.
- 디자인: 초록·카키 테마, UI 글꼴은 Pretendard, 송폼 글꼴은 함초롬바탕(`HCR Batang`, 없으면 바탕 → Noto Serif KR), 다크 모드 토큰을 쓴다.

## 키 바꾸기 구조 (가장 손이 많이 간 부분)

1. `findStaves`: 가로로 어두운 픽셀이 50% 이상인 행을 오선으로 보고, 전형적인 줄 간격(`gap`)으로 5줄씩 묶는다. 밝기 기준은 200이다(흐린 스캔 대응).
2. `toGray`: 흑백 변환과 대비 확장을 한다. 형광펜 색을 없앤다.
3. OCR 1차는 페이지 전체를 PSM 11로 읽는다(`gap`이 작으면 확대).
4. 2차는 오선마다 바로 위의 **코드 띠**를 뗀 다음, `cropChordRow`로 **띠 안에서 코드 행만 다시 잘라낸다.** 띠에는 윗단의 가사·마디 번호·도돌이 괄호가 같이 들어오는데, 이걸 버리는 것이 오인식을 줄이는 가장 큰 요인이다. 잘린 코드 행은 글자가 약 40px이 되게 키워 **PSM 7**(한 줄)로 읽는다.
   - **띠의 맨 아래 행은 코드가 아니다.** 실제 악보에는 오선 위로 솟은 기둥·빔과 높은음자리표가 항상 그 자리에 찍힌다. "마지막 텍스트 행"을 코드행으로 보면 한 칸 아래를 집는다. 아래 모서리에 닿는(`y1 >= 높이-2`) 행은 다른 후보가 남아 있는 한 버린다. **이 한 줄이 1400px에서 7/26을 26/26으로 바꿨다.**
   - 그 밖에 가로로 폭 전체에 이어진 잉크(오선·괄호)와 글자 높이(`gap*0.9`)에 못 미치는 조각도 뺀다.
5. 저해상도(`gap < 9`)면 2차를 세 가지 시야(psm7@40px, psm6@56px, psm11@40px)로 읽고 `mergeRuns`가 가로 겹침으로 묶어 신뢰도 합이 큰 읽기를 고른다. 이때 **코드로 해석되는 읽기끼리만 투표**한다(쓰레기 읽기가 신뢰도만 높다고 이기면 토큰째 버려진다).
6. `crossVote`: 한 장에는 같은 코드가 여러 번 찍힌다. 토큰 픽셀을 32×16으로 정규화한 "도장"으로 만들어 상관계수 0.88 이상이면 한 무리로 묶고(가로/세로 비가 18% 넘게 다르면 제외 — G와 Em7을 가르는 값이다), 무리 전체에 신뢰도 합이 큰 읽기를 준다. **같은 G가 줄마다 다르게 읽히는 문제를 여기서 잡는다.** 읽기가 바뀐 토큰은 `redraw`가 붙어 원본 픽셀을 재활용하지 않고 통째로 다시 그린다.
7. `wordsToTokens`: 아래 오선까지의 거리가 위 오선까지의 거리보다 가까워야 코드로 본다(가사 제외). 오선 왼쪽의 마디 번호를 거르고, A~G로 시작해야 한다.
8. `splitGlued`: `Esus4 E7`처럼 가까이 찍힌 두 코드가 한 덩어리로 읽힌다. **그 자체로는 코드가 아니고 쪼갠 양쪽이 둘 다 코드일 때만** 나눈다(진짜 코드는 그렇게 쪼개지지 않는다).
9. `parseChord`: 사용자가 준 12키 코드표 문법(3화음, sus4, aug, dim, M7, 7, m7, 6, m6, m7(b5), dim7)과 슬래시 베이스를 지원한다. `known`이 false면 노란 상자로 표시한다.
10. `guessKeyFromTokens`: 12키를 모두 대보고 읽은 코드를 가장 잘 설명하는 키를 고른다. **화음의 장단까지 본다** — 근음만 보면 A조 악보를 G조로 짚는다(A장3화음이 G조의 ii라면 단3화음이어야 한다). 읽기와 키가 서로 물려 있어서(잘못 읽은 글자는 키를 가정해야 고쳐진다) 키마다 `mendForKey`를 걸어보고 채점한다.
11. `mendForKey`: OCR은 ♯을 `E`·`2`·`g`·`s`로 읽고 슬래시를 흘린다(`F#m7`→`FEm7`, `A/C#`→`ACs`). **고친 결과가 그 키에 속하고 그 키의 표기법과 맞을 때만** 되돌린다. 이 조건이 없으면 `As` 같은 글자가 없는 코드를 지어낸다.
12. `repairForKey`: OCR은 ♭/♯을 자주 빠뜨린다. 원래 키에서 벗어난 맨글자인데 그 ♭/♯ 짝이 강한 음도(I ii iii IV V vi)라면 짝으로 고친다(예: B♭키에서 B → B♭). 원래 키를 바꾸면 다시 계산한다.
13. 전조 표기: 목표 키가 G·D·A·E·B면 #, 그 밖(C 포함)은 ♭을 쓴다.
14. 상자 색: 초록은 읽음, 노랑은 키에 맞춰 고쳤거나·교차 투표로 바꿨거나·확인이 필요함, 빨강은 못 읽음이다. 상자를 누르면 고치거나 지울 수 있고, 빈 곳을 누르면 추가된다.

## 측정 (dev/ 벤치)

`dev/`에 브라우저에서 도는 벤치가 있다. 배포 사이트가 아니라 로컬 서버(`/run` 또는 `.claude/launch.json`의 `conti`)로 열어야 CDN이 막히지 않는다.

### 실제 악보 (가장 믿을 숫자)

`samples/`에 둔 실제 스캔 두 장을 줄여 가며 잰 것이다. **사용자가 키를 고쳐주지 않은 상태**의 자동 결과다. 악보는 저작권이 있어 `.gitignore`에 넣었으니, 저장소만 받은 사람은 직접 넣어야 한다.

| 곡 | 가로 | 기존 | 지금 |
| --- | --- | --- | --- |
| 주 행하신 위대한 일 (코드 26) | 640px | 18/26 (69%) | **26/26 (100%)** |
| | 1400px | 15/26 (58%) | **26/26 (100%)** |
| 내 구주 예수님 (코드 53) | 640px | 24/53 (45%) | **37/53 (70%)** |
| | 1400px | 28/53 (53%) | **37/53 (70%)** |

원래 키 자동 추측도 기존에는 두 곡 다 틀렸고(C, D), 지금은 둘 다 맞힌다(G, A).

`내 구주 예수님`이 낮은 건 `A/C#`, `Bm7/F#`, `Esus4 E7`, `E/G#`처럼 슬래시 베이스와 붙어 있는 코드가 많아서다. 남은 오류도 대부분 거기에 몰려 있다.

### 생성 악보 (전처리 실험용)

아래 숫자는 생성한 악보로 잰 것이라 실제 스캔과는 다르게 나온다. 전처리 선택지를 가릴 때만 쓸 것.

- `ocr-bench.html` — 확대 방식 비교. 리샘플링을 정교하게(nearest+샤픈, Sauvola 이진화, 단계적 축소) 바꾸는 건 **효과가 없거나 더 나빴다.** 지금의 평범한 smooth 확대가 제일 낫다.
- `ocr-bench2.html` — 띠 전체 vs 코드행 크롭 × PSM. 가사가 섞인 띠에 PSM 7을 그냥 쓰면 **4/28로 무너진다**(옛 기록이 "PSM 6/7은 더 나빴다"고 한 게 이 경우다). 코드행을 먼저 자르면 PSM 7이 제일 낫고, 오인식이 12px에서 14건→0건, 9px에서 20건→4건으로 줄었다.
- `ocr-bench3.html` — 확대 목표를 40px보다 키워도(56·72px) 나아지지 않는다. 세 시야 앙상블은 7px에서 14→17, 9px에서 21→23.
- `ocr-bench4.html` — 교차 투표로 7px에서 13→16, `Fm7→Em7`·`Dn→D`처럼 의도한 보정이 찍힌다.
- `ocr-bench5.html` — 띠를 떼고 코드행을 자르는 과정을 행 단위로 찍어 본다. `?img=/samples/ju.png&w=1400`을 붙이면 실제 악보로 돈다. **크롭이 이상하면 먼저 이 페이지를 볼 것** — 코드행 크롭 버그 두 개를 여기서 잡았다.
- `e2e.js` — 앱 UI를 그대로 몰아서 돌리고 채점한다. `index.html`에서 `fetch('/dev/e2e.js').then(r=>r.text()).then(eval)` 후 `__runURL('/samples/ju.png', 1400, 'ju')` → `__score()`. 정답표는 이 파일 안에 있다.

생성 악보 한 장(코드 17개)으로는 620px에서 14/17 → 15/17, 900·1400px에서 17/17이 나온다. 다만 이 악보는 오선 위로 솟는 기둥·빔이 없어서 실제 스캔의 어려움을 재현하지 못한다 — 위의 크롭 버그도 여기서는 드러나지 않았다.

## 알려진 한계 / 버린 시도

- **해상도만으로 설명되지 않는다.** 실제 악보에서는 640px과 1400px 결과가 거의 같았다. 크롭을 고치기 전에는 오히려 1400px이 더 나빴다. 해상도를 탓하기 전에 `ocr-bench5.html`로 크롭부터 볼 것. 1,400px 이상을 권하는 안내는 그대로 두되, 저해상도가 원인이라고 단정하지 말 것.
- 확대 배율을 더 올리는 것(×5~8, 목표 56·72px), 이진화(threshold·Sauvola), nearest+언샵 마스크는 모두 측정해서 버렸다. whitelist `ABCDEFGmajdinsugb#/()0123456789+-M`는 유지한다.
- `user_defined_dpi: '300'`은 빼면 620px에서 15→14로 떨어져서 넣어 뒀다.
- **PSM 7은 코드행을 자른 뒤에만 쓸 것.** 띠 전체에 쓰면 여러 줄을 한 줄로 뭉개서 거의 못 읽는다.
- 글자 오른쪽의 잉크를 보고 ♭ 누락을 판정하는 방식은 오탐이 너무 많아 버렸고, 키 기반 보정(`repairForKey`)으로 대체했다.
- 원래 키 자동 추측이 틀릴 수 있다(B♭ 곡을 F로 짚은 적이 있다). UI에서 사용자에게 확인시킨다.
- Claude.ai 아티팩트 미리보기에서는 CSP 때문에 tesseract와 pdf.js가 로드되지 않는다. 배포 사이트에서만 동작한다.

## 다음 후보

- **슬래시 베이스가 남은 약점이다.** `A/C#`이 `As`·`A/C`로, `E/G#`이 `B/G`로 읽힌다. 슬래시가 작아서 자주 사라지고, 그 뒤 ♯도 같이 뭉갠다. 베이스 자리만 따로 잘라 확대해 다시 읽는 방향.
- `Esusd4E7`처럼 **붙은 데다 글자까지 깨진** 덩어리는 `splitGlued`가 못 쪼갠다(양쪽이 코드여야 나눈다). 연결 성분으로 글자를 나눈 뒤 개별 인식하는 방향.
- 악보 맨 앞의 조표를 코드로 읽는 경우가 있다(`C#`, `A4`). 오선 시작점에서 조표 폭만큼 더 들어간 뒤부터 코드로 보면 줄어든다.
- 보관함 기기 간 공유(지금은 브라우저마다 따로): Supabase 같은 저장소 검토.
