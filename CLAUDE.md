# 콘티 한 장

Covenant 청년부 찬양팀이 매주 쓰는 A4 찬양 콘티 편집기다. 서버 없이 `index.html` 한 파일로 돌아가는 정적 사이트다.

- 배포: https://conti-han-jang.vercel.app (Vercel 개인 Hobby 계정 `slzp300-sketch's projects`, **ImDealer 팀 아님**)
- 저장소: https://github.com/slzp300-sketch/conti-han-jang (`main` push → 자동 배포)
- 로컬: `C:\Users\upup0\Documents\conti-han-jang`
- 배포: `git add -A && git commit -m "..." && git push`, 또는 `배포하기.bat` 더블클릭

## 파일

- `index.html` — 앱 전부(HTML·CSS·JS 한 파일). 사이트용 빌드라서 Pretendard를 CDN에서 불러온다.
- `배포하기.bat` — add/commit/push를 한 번에 실행한다.
- `vercel.json`, `README.md`, `.gitignore`

## 기능

- 곡마다 **Song Form** 칸과 악보 이미지를 넣으면 A4 미리보기가 나온다. 인쇄하거나 PDF로 저장할 수 있다.
- 송폼 표기: `{C(목소리)}`처럼 중괄호로 감싸면 빨간색이 되고, `(괄호)` 안의 글자는 작게 나온다. `*`는 `B*6` 같은 반복 표기라서 **그대로 둔다**(예전에는 빨간색 표기였는데 사용자 요청으로 바꿨다).
- 악보 넣기는 이미지, **PDF**(pdf.js로 쪽마다 가로 2000px 렌더링), 드래그, Ctrl+V를 모두 지원한다.
- 자르기(✂): 악보 위·아래를 %로 잘라낸다. 원본은 두고 값만 저장한다.
- 두 곡 한 장: 곡마다 `joinPrev` 값이 있고, 켜면 자른 뒤 남은 높이 비율로 페이지를 나눈다.
- 보관함: IndexedDB `conti-maker`에 저장한다. store는 `state`(key `current`)와 `contis`(keyPath `id`) 두 개다. 새 콘티·열기·복제·삭제를 지원하고, 데이터는 브라우저마다 따로 저장돼 공유되지 않는다.
- **키 바꾸기(♯)**: tesseract.js 5.1.1(CDN)로 코드를 인식하고, 캔버스에 루트와 베이스 글자만 다시 그린다.
- 디자인: 초록·카키 테마, UI 글꼴은 Pretendard, 송폼 글꼴은 함초롬바탕(`HCR Batang`, 없으면 바탕 → Noto Serif KR), 다크 모드 토큰을 쓴다.

## 키 바꾸기 구조 (가장 손이 많이 간 부분)

1. `findStaves`: 가로로 어두운 픽셀이 50% 이상인 행을 오선으로 보고, 전형적인 줄 간격(`gap`)으로 5줄씩 묶는다. 밝기 기준은 200이다(흐린 스캔 대응).
2. `toGray`: 흑백 변환과 대비 확장을 한다. 형광펜 색을 없앤다.
3. OCR 1차는 페이지 전체를 PSM 11로 읽는다(`gap`이 작으면 확대).
4. 2차는 오선마다 바로 위의 **코드 띠**를 뗀 다음, `cropChordRow`로 **띠 안에서 코드 행만 다시 잘라낸다.** 띠에는 윗단의 가사·마디 번호·도돌이 괄호가 같이 들어오는데, 이걸 버리는 것이 오인식을 줄이는 가장 큰 요인이다. 가로로 이어진 잉크(오선)와 글자 높이(`gap*0.9`)에 못 미치는 조각은 텍스트 행에서 뺀다. 잘린 코드 행은 글자가 약 40px이 되게 키워 **PSM 7**(한 줄)로 읽는다.
5. 저해상도(`gap < 9`)면 2차를 세 가지 시야(psm7@40px, psm6@56px, psm11@40px)로 읽고 `mergeRuns`가 가로 겹침으로 묶어 신뢰도 합이 큰 읽기를 고른다. 이때 **코드로 해석되는 읽기끼리만 투표**한다(쓰레기 읽기가 신뢰도만 높다고 이기면 토큰째 버려진다).
6. `crossVote`: 한 장에는 같은 코드가 여러 번 찍힌다. 토큰 픽셀을 32×16으로 정규화한 "도장"으로 만들어 상관계수 0.88 이상이면 한 무리로 묶고(가로/세로 비가 18% 넘게 다르면 제외 — G와 Em7을 가르는 값이다), 무리 전체에 신뢰도 합이 큰 읽기를 준다. **같은 G가 줄마다 다르게 읽히는 문제를 여기서 잡는다.** 읽기가 바뀐 토큰은 `redraw`가 붙어 원본 픽셀을 재활용하지 않고 통째로 다시 그린다.
7. `wordsToTokens`: 아래 오선까지의 거리가 위 오선까지의 거리보다 가까워야 코드로 본다(가사 제외). 오선 왼쪽의 마디 번호를 거르고, A~G로 시작해야 한다.
8. `parseChord`: 사용자가 준 12키 코드표 문법(3화음, sus4, aug, dim, M7, 7, m7, 6, m6, m7(b5), dim7)과 슬래시 베이스를 지원한다. `known`이 false면 노란 상자로 표시한다.
9. `repairForKey`: OCR은 ♭/♯을 자주 빠뜨린다. 원래 키에서 벗어난 맨글자인데 그 ♭/♯ 짝이 강한 음도(I ii iii IV V vi)라면 짝으로 고친다(예: B♭키에서 B → B♭). 원래 키를 바꾸면 다시 계산한다.
10. 전조 표기: 목표 키가 G·D·A·E·B면 #, 그 밖(C 포함)은 ♭을 쓴다.
11. 상자 색: 초록은 읽음, 노랑은 키에 맞춰 고쳤거나·교차 투표로 바꿨거나·확인이 필요함, 빨강은 못 읽음이다. 상자를 누르면 고치거나 지울 수 있고, 빈 곳을 누르면 추가된다.

## 측정 (dev/ 벤치)

`dev/`에 브라우저에서 도는 벤치가 있다. 배포 사이트가 아니라 로컬 서버(`/run` 또는 `.claude/launch.json`의 `conti`)로 열어야 CDN이 막히지 않는다. 아래 숫자는 전부 **생성한 악보**로 잰 것이고, 실제 스캔 악보로는 아직 검증하지 않았다.

- `ocr-bench.html` — 확대 방식 비교. 리샘플링을 정교하게(nearest+샤픈, Sauvola 이진화, 단계적 축소) 바꾸는 건 **효과가 없거나 더 나빴다.** 지금의 평범한 smooth 확대가 제일 낫다.
- `ocr-bench2.html` — 띠 전체 vs 코드행 크롭 × PSM. 가사가 섞인 띠에 PSM 7을 그냥 쓰면 **4/28로 무너진다**(옛 기록이 "PSM 6/7은 더 나빴다"고 한 게 이 경우다). 코드행을 먼저 자르면 PSM 7이 제일 낫고, 오인식이 12px에서 14건→0건, 9px에서 20건→4건으로 줄었다.
- `ocr-bench3.html` — 확대 목표를 40px보다 키워도(56·72px) 나아지지 않는다. 세 시야 앙상블은 7px에서 14→17, 9px에서 21→23.
- `ocr-bench4.html` — 교차 투표로 7px에서 13→16, `Fm7→Em7`·`Dn→D`처럼 의도한 보정이 찍힌다.
- `ocr-bench5.html` — 실제 페이지에서 띠를 떼고 코드행을 자르는 과정을 눈으로 본다. **코드행 바로 아래의 희미한 4px 잉크 조각(오선 윗변)이 "마지막 텍스트 행"으로 뽑히던 버그를 여기서 잡았다.** 크롭이 이상하면 먼저 이 페이지를 볼 것.
- `e2e.js` — 앱 UI를 그대로 몰아서 돌리는 스크립트. `index.html`에서 `fetch('/dev/e2e.js').then(r=>r.text()).then(eval)` 후 `__run(620)` → `__score()`.

생성 악보 한 장(코드 17개)을 신·구 코드로 돌린 결과:

| 가로 | 기존 | 지금 |
| --- | --- | --- |
| 620px | 14/17 | 15/17 |
| 900px | 10/17 | 17/17 |
| 1400px | 7/17 | 17/17 |

세 조건 모두 잘못 읽은 상자는 0개였다.

## 알려진 한계 / 버린 시도

- **해상도가 여전히 가장 크다.** 가로 600px 안팎이면 코드 글자가 7~10px이라 위 표처럼 몇 개는 못 찾는다. 1,400px 이상이나 PDF를 권하는 안내는 그대로 둔다.
- 확대 배율을 더 올리는 것(×5~8, 목표 56·72px), 이진화(threshold·Sauvola), nearest+언샵 마스크는 모두 측정해서 버렸다. whitelist `ABCDEFGmajdinsugb#/()0123456789+-M`는 유지한다.
- `user_defined_dpi: '300'`은 빼면 620px에서 15→14로 떨어져서 넣어 뒀다.
- **PSM 7은 코드행을 자른 뒤에만 쓸 것.** 띠 전체에 쓰면 여러 줄을 한 줄로 뭉개서 거의 못 읽는다.
- 글자 오른쪽의 잉크를 보고 ♭ 누락을 판정하는 방식은 오탐이 너무 많아 버렸고, 키 기반 보정(`repairForKey`)으로 대체했다.
- 원래 키 자동 추측이 틀릴 수 있다(B♭ 곡을 F로 짚은 적이 있다). UI에서 사용자에게 확인시킨다.
- Claude.ai 아티팩트 미리보기에서는 CSP 때문에 tesseract와 pdf.js가 로드되지 않는다. 배포 사이트에서만 동작한다.

## 다음 후보

- **실제 스캔 악보로 검증.** 위 숫자는 전부 생성한 악보 기준이다. "내 구주 예수님", "주 행하신 위대한 일"을 저해상도로 줄여 `dev/e2e.js`와 같은 방식으로 재볼 것.
- 저해상도에서 남는 오류는 **글자 붙음**이다(예: `CG`처럼 두 코드가 한 덩어리로 읽힘). 투표는 라벨만 고치지 덩어리를 못 쪼갠다. 연결 성분으로 나눈 뒤 개별 인식하는 방향.
- 보관함 기기 간 공유(지금은 브라우저마다 따로): Supabase 같은 저장소 검토.
